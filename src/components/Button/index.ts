import IncludingImgButton from './IncludingImgButton';
import PrimaryButton from './PrimaryButton';

export const Button = Object.assign(PrimaryButton, {
  IncludingImgButton: IncludingImgButton,
});
